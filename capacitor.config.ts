import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bilibov.vue.memories',
  appName: 'Vue Memories',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
